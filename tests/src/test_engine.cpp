#include "CppUTest/TestHarness.h"

#include "engine.hpp"

TEST_GROUP(Engine){};

TEST(Engine, create_engine) {
	/*struct orm::database_uri db_uri;
	db_uri = orm::parse_database_uri("dialect://username:pasword@server/db");
	CHECK_EQUAL((std::string)"dialect", db_uri.dialect);
	CHECK_EQUAL((std::string)"username", db_uri.username);
	CHECK_EQUAL((std::string)"password", db_uri.password);
	CHECK_EQUAL((std::string)"server", db_uri.host);
	CHECK_EQUAL((std::string)"db", db_uri.database);

	db_uri = orm::parse_database_uri("dialect:///db");
	CHECK_EQUAL((std::string)"dialect", db_uri.dialect);
	CHECK_EQUAL((std::string)"db", db_uri.database);*/
}

